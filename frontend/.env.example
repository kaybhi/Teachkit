# Copy to .env for local dev. CRA bakes this in at build time — for a
# deployed build, set it as a build-time env var/ARG on your host instead
# of relying on a committed .env.
REACT_APP_BACKEND_URL=http://localhost:8000
